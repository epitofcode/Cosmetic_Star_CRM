{
  "name": "cosmetic-star-root",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "echo 'Frontend built on Vercel'",
    "install-all": "npm install",
    "start": "cd backend && node server.js"
  }
}
